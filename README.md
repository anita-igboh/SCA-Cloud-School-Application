# SCA Cloud School Application

This Project, Create/Build a Command-line interface (CLI) application which will install the following softwares: wget, curl, Node.js

Developed by anitaigboh@gmail.com
- Version 0.0.0: 14th January 2021.

The project was auto created using NodeJS Scripting language.
-- DOCUMENTATION --

In the project directory, you can run:
### `npm i -g`
Installs the app globally.<br>

### `scsa`
This command runs the CLI to perfomr multiple fucntions.<br>

Swagger UI for API documentation.
### `scsa -i`
Runs the installer to install to the system.<br>

